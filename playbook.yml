- name: Setup
  hosts: localhost
  vars:
    fzf_version: 0.32.1
    neovim_version: 0.8.3
    session_user: "{{ ansible_user_id }}"

  roles:
    - apt
    - vim
    - git
    - starship
    - antigen
    - fzf
    - zsh
    - ripgrep
    - path
    - neovim
    - unzip
    - golang
    - tmux
